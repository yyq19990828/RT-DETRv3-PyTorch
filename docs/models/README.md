# 模型文档

本目录按 DETR 架构组织模型专属文档。跨模型复用的配置、训练、权重转换和排错经验仍保存在 [`docs/migrations`](../migrations/README.md)，这里不重复这些公共合同。

## 已收录模型

- [RT-DETRv3](rtdetrv3/README.md)：当前仓库首个且唯一完成收录的模型，包含配置支持、CLI/导出边界、已知限制和 `v0.1.0` 验证证据入口。

新增模型时，应创建一个同级模型目录；不要在每个迁移主题下重复建立模型子目录。
