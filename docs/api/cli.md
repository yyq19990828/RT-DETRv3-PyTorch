# 命令行入口

`detrs.cli` 实现唯一的 `detrs` 命令(亦可用 `python -m detrs`),子命令为 `train`、`eval`、`infer`、`export`、`convert` 和 `models`。未迁移的 Paddle CLI 参数会明确报错,不会静默忽略。

::: detrs.cli.main

::: detrs.cli.train

::: detrs.cli.eval

::: detrs.cli.infer

::: detrs.cli.export

::: detrs.cli.convert

::: detrs.cli.models
